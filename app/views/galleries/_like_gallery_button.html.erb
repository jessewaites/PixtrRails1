<% if current_user.likes? @gallery %>
  <%= button_to "Unlike Gallery", unlike_gallery_path(@gallery), class: "button radius", remote: true, method: :delete %>
<% else %>
  <%= button_to "Like Gallery", like_gallery_path(@gallery), class: "button radius", remote: true %>
<% end %>